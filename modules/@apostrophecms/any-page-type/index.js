module.exports = {

  /*
  extend: '@apostrophecms/any-page-type',
  extendMethods(self) {
    return {
      getBrowserData(_super, req) {
        const browserData = _super(req);
        browserData.version = 1;
        return browserData;
      }
    };

  }
  */
};
